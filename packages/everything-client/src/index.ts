import { createClient } from "./client";
import type { ClientOptions, EverythingClient } from "./types";
import type { SearchOptions, SearchResult, SearchStatus } from "./types";

export { createClient };
export type {
  ClientOptions,
  EverythingClient,
  SearchOptions,
  SearchResult,
  SearchStatus,
};
